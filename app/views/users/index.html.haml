%h1 Members
%br
%ul.thumbnails
  - @users.each do |user|
    %li.span2
      .thumbnail
        - if user.avatar && user.avatar.image_url(:thumb)
          = link_to_image user.avatar.image_url(:thumb).to_s, {}, user_path(user)
        -else
          = image_tag 'default.png'
        .caption
          %h3= user.name
          -if user.bio
            %p= user.bio